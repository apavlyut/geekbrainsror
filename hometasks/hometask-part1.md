## 1 часть. Овтетьте на вопросы и прокомментируйте почему так происходит.

> Для выполнения задания копируете это себе через fork. В поле "ответ" указываете ответ, в поле "почему" пишете ваше понимание почему это так работает с точки зрения языка.

1) Присвойте переменной x значение 5.

```ruby
# ответ (пример):
x = 5

# почему: все так потому что присвоение в языке идет через одиночный знак равенства, порядок присвоения идет справа налево.
```

2) Какой тип данных имеет это выражение?

```ruby
"Bob"

# ответ: строка
# почему: потому что в ""
```

3) x это строка?

```ruby
x = 5
# ответ: нет, целое число
# почему: потмоу что Fixnum
```

4) Что будет результатом выполнения?

```ruby
"HEL" + "LO"
# ответ: HELLO
# почему: добавление строки в конце другой
```

5) Что будет результатом выполнения?

```ruby
"Hel".+("lo")
# ответ: Hello
# почему: потмоу что метод один и тот же, разное написание
```

6) Что будет результатом выполнения?

```ruby
"Hel" + 42
# ответ: ошибка
# почему: разные типы данных
```

7) Что будет результатом выполнения?

```ruby
a = "OH"
b = "mygod"
a + b
# ответ: OHmygod
# почему: сложение переменных с содержимым с типом строка добавляет одну строку к другой
```

8) Что выведет на экран?

```ruby
my_role = "Teacher"
my_role = "Student"

p my_role
# ответ: Student
# почему: выводится последнее заданное значение
```

9) Получите первую букву из строки:

```ruby
"GeekBrains"
# ответ: "GeekBrains"[0]
# почему: каждый символ строки имеет свой индекс
```

10) Получите с первой по треьюю букву из строки:
```ruby
"GeekBrains"
# ответ: "GeekBrains" [0..2]
# почему: аналогично предыдущему, только указал интервал
```

11) Получите последнюю букву из строки.

```ruby
"GeekBrains"
# ответ: "GeekBrains" [-1]
# почему: тоже самое только с конца слова
```

12) Замените букву b на d в строке:

```ruby
"Geekbrains"
# ответ: "Geekbrains".sub(/b/, 'd')
# почему: sub возвращает копию строки с соответсвующими изменениями
```

13) Присвойте значение "хеллоу" переменной howru?

```ruby
# ответ: howru = "хеллоу"
# почему: переменной пристваивается значение
```

14) Что будет результатом выполнения?

```ruby
"Пьяный мастер" = never_sleeps
# ответ: синтактическая ошибка
# почему: потому что чтение идет слева направо
```

15) Что будет выведено на экран?

```ruby
chtoto = "budet"
sosvem = netak
puts sosvem
# ответ: ошибка nil
# почему: значение переменной sosvem присвоено в виде переменной netak, которой значение не присвоено вообще
```

16) Какой будет результат?

```ruby
3 + 4
# ответ: 7
# почему: сложение целых чисел
```

17) Какой будет результат?

```ruby
4 * 7
# ответ: 28
# почему: аналогично умножение
```

18) Какой будет результат?

```ruby
2 ** 3
# ответ: 8
# почему: два в третьей степени
```

19) Какой будет результат?

```ruby
8 / 2
# ответ: 4
# почему: операция деления
```

20) Какой будет результат?

```ruby
3 / 2
# ответ: 1
# почему: выводится целое число
```

21) Какой будет результат?

```ruby
3.0 / 2.0
# ответ: 1.5
# почему: деление с плавающей запятой, здесь ничего не отбрасывается
```

22) Какой будет результат?

```ruby
"i am not afraid".upcase()
# ответ: I AM NOT AFRAID
# почему: метод upcase меняет все символы на заглавные
```

23) Переведите в нижний регистр.

```ruby
what = "What ArE yOuAR?"
# ответ: "What ArE yOuAR?".downcase
# почему: метод .downcase опускает все символы в нижний регистр
```

24) Конкатенируйте строки:

```ruby
what = "Прекраная "
isthis = "годнота то"
# ответ: what + isthis => Прекраная годнота то
# почему: складываем переменные значения которых являются строками
```
25) Переведите число 5 в строку.

```ruby
# ответ: 5.to_s
# почему: метод to_s конвертирует в строку
```

26) Что тут за проблемы?

```ruby
my name = "Mr. Stupid"
# ответ: ошибка
# почему: переменная my name задана неверно. это две разных переменных my name
```

27) Исправьте код чтобы он выполнился:

```ruby
band = "City" + 312
# ответ: band = "City" + 312.to_s
# почему: сложение разных типов не выполнится, поэтому 312 приводится к строке
```

28) my_array - массив из значений "ruby" and "rails". Создайте свой массив с такими же значениями.

```ruby
# ответ: my_array = ['ruby', 'rails']
# почему: так можно присвоить переменной массив
```

29) Создайте пустой массив:

```ruby
# ответ: my_array = []
# почему: [] дает понять, что это массив
```

30) Создайте массив содержащий значения "алекс мерфи", 33, и 12.5.
```ruby
# ответ: my_array = ["алекс мерфи", 33, 12.5]
# почему: поддерживаются разные типы данных
```

31) Опишите компоненты этого выражения:

```ruby
users = ["john", "alex", "vlad"]
# ответ: users - переменная которая становится массивом, = присваивание, "john", "alex", "vlad" значения внутри массива
# почему:переменной присвоен массив
```

32) Что будет результатом выражения:

```ruby
["john", "alex", "vlad"].length()
# ответ: 3
# почему: length() выводит количество элементов
```

33) Массив корректный?

```ruby
name = "sir"
age = 32
[name, age]
# ответ: да
# почему: поддерживаются разные типы данных
```

34) Получите первый элемент массива.

```ruby
["john", "alex", "vlad"]
# ответ: ["john", "alex", "vlad"].first
# почему: first выводит первые или первые n (first(n)) элемены массива
```

35) Получите последний элемент массива.

```ruby
["john", "alex", "vlad"]
# ответ: ["john", "alex", "vlad"].last
# почему: аналогично предыдущему использован метод last
```

36) Добавьте строку "я молодец" в конец массива:

```ruby
["кто", "молодец", "?"]
# ответ:   ["кто", "молодец", "?"].push("я молодец")
# почему: push добавляет элемент в конец массива
```

37) Что будет результом выполнения?

```ruby
5 == 5
# ответ: true
# почему: == - это сравнение
```

38) Что будет результом выполнения?

```ruby
true = 6
# ответ: ошибка
# почему: true нельзя использовать для названия переменной
```

39) Продемонстрируйте что "руби" полностью эквивалентен "руби".

```ruby
# ответ: руби"=="руби"
# почему: сравнение
```

40) Что будет результом выполнения?

```ruby
4 != 4
# ответ: false
# почему: != тк. элемены совпадают, а не должны. иначе было бы true
```

41) Что будет результом выполнения?

```ruby
5 > 4
# ответ: true
# почему: все верно, 5 больше 4
```

42) Что будет напечатано?

```ruby
if 5 > 4
  puts "5 больше 4"
end
# ответ: "5 больше 4"
# почему: все верно, условие выполнено
```

43) Что будет напечатано?

```ruby
if 5 < 4
  puts "5 меньше 4"
else
  puts "5 не меньше 4"
end

# ответ: "5 не меньше 4"
# почему: т.к. условие не выполнкно
```

44) Что будет напечатано?

```ruby
if "алекс" == "шмалекс"
  puts "алекс это шмалекс"
elsif "череп" == "череп"
  puts "череп и есть череп"
else
  puts "что-то пошло не так"
end
# ответ: "череп и есть череп"
# почему: это слвоие выполнено
```

45) Что будет напечатано?

```ruby
if 5
  puts "Ну привет"
end
# ответ: Ну привет    
# почему: потому чт овсе верно, 5 - это и есть 5
```

46) Что будет напечатано?

```ruby
if "ruby"
  puts "ruby это круто"
else
  puts "ruby все равно круто"
end

#ответ: ruby это круто
# почему: аналогично предыдущему
```

47) Что будет напечатано?

```ruby
if nil
  puts "и правда nil"
else
  puts "насильно nil не будешь"
end
# ответ: насильно nil не будешь
# почему: потмоу что nil false
```

48) Что будет напечатано?

```ruby
puts "Так тоже можно печатать логические проверки" if true
# ответ: Так тоже можно печатать логические проверки
# почему: не важно в каком месте строки условие
```

49) Что будет напечатано?

```ruby
puts "Так тоже можно проверять если коротко" if false
# ответ: не будет ничего
# почему: потому что false
```

50) Какое будет значение X?

```ruby
x = 5
x = x + 1
# ответ: 6
# почему: потмоу чт опоследние значение было 5+1
```

51) Какое будет значение helloer?

```ruby
helloer = "chill"
helloer += "hippie"
# ответ: chillhippie
# почему: сложили же
```

52) Что будет напечатано?

```ruby
counter = 0
while counter < 3
  puts "Поехали кружиться"
  counter = counter + 1
end
# ответ: поехали кружиться
# поехали кружиться
# поехали кружиться
# почему: цикл с условием
```

53) Что будет напечатано?

```ruby
while 3 > 15
  puts "Ну все приплыли"
end
# ответ: ничего н ебудет  
# почему: counter забыли
```

54) Что будет напечатано?

```ruby
while true
  puts "Бесконечность тоже конечна, только мы об этом не всегда можем знать."
end
# ответ: бесконечное количество раз Бесконечность тоже конечна, только мы об этом не всегда можем знать.
# почему: потому что условие такое
```
