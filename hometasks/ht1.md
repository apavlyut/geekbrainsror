## 1 часть. Овтетьте на вопросы и прокомментируйте почему так происходит.

> Для выполнения задания копируете это себе через fork. В поле "ответ" указываете ответ, в поле "почему" пишете ваше понимание почему это так работает с точки зрения языка.

1) Присвойте переменной x значение 5.

```ruby
# ответ (пример):
x = 5

# почему: все так потому что присвоение в языке идет через одиночный знак равенства, порядок присвоения идет справа налево.
```

2) Какой тип данных имеет это выражение?

```ruby
"Bob"

# ответ:String
# почему:"в кавычках строки"
```

3) x это строка?

```ruby
x = 5
# ответ:нет
# почему:Fixnum - переменная
```

4) Что будет результатом выполнения?

```ruby
"HEL" + "LO"
# ответ:"HELLO"
# почему:это конкатенация
```

5) Что будет результатом выполнения?

```ruby
"Hel".+("lo")
# ответ:"Hello"
# почему:это конкатенация
```

6) Что будет результатом выполнения?

```ruby
"Hel" + 42
# ответ:TypeError:
# почему: нельзя склеить различные типы
```

7) Что будет результатом выполнения?

```ruby
a = "OH"
b = "mygod"
a + b
# ответ:"OHmygod"
# почему:это конкатенация
```

8) Что выведет на экран?

```ruby
my_role = "Teacher"
my_role = "Student"

p my_role
# ответ:"Student"
# почему: последнее присвоенное значение - Student
```

9) Получите первую букву из строки:

```ruby
"GeekBrains"
# ответ:a[0]
# почему:первая буква - нулевая
```

10) Получите с первой по треьюю букву из строки:
```ruby
"GeekBrains"
# ответ:a[0..2] or a[0...3]
# почему:выбираем диапазон массива
```

11) Получите последнюю букву из строки.

```ruby
"GeekBrains"
# ответ:a[-1]
# почему:выбираем последний элемент
```

12) Замените букву b на d в строке:

```ruby
"Geekbrains"
# ответ:a[4] = 'd'
# почему:задаем элемент
```

13) Присвойте значение "хеллоу" переменной howru?

```ruby
# ответ:SyntaxError
# почему:howru? - не переменная
It is a code style convention; it indicates that a method returns a boolean value.

The question mark is a valid character at the end of a method name.
```

14) Что будет результатом выполнения?

```ruby
"Пьяный мастер" = never_sleeps
# ответ:SyntaxError
# почему:нет такой команды
```

15) Что будет выведено на экран?

```ruby
chtoto = "budet"
sosvem = netak
puts sosvem
# ответ:nil
# почему:переменная netak содержит nil
```

16) Какой будет результат?

```ruby
3 + 4
# ответ:7
# почему:Fixnum+Fixnum
```

17) Какой будет результат?

```ruby
4 * 7
# ответ:28
# почему:Fixnum*Fixnum
```

18) Какой будет результат?

```ruby
2 ** 3
# ответ:8
# почему:** означает  степень - math
```

19) Какой будет результат?

```ruby
8 / 2
# ответ:4
# почему:Fixnum/Fixnum
```

20) Какой будет результат?

```ruby
3 / 2
# ответ:1
# почему:3.to_f/2=1.5 (no float)

```

21) Какой будет результат?

```ruby
3.0 / 2.0
# ответ:1.5
# почему:float number
```

22) Какой будет результат?

```ruby
"i am not afraid".upcase()
# ответ:"I AM NOT AFRAID"
# почему:string method
```

23) Переведите в нижний регистр.

```ruby
what = "What ArE yOuAR?"
# ответ:what.downcase
# почему:string method
```

24) Конкатенируйте строки:

```ruby
what = "Прекраная "
isthis = "годнота то"
# ответ:what + isthis
# почему:String+String
```
25) Переведите число 5 в строку.

```ruby
# ответ:5.to_s
 => "5" 
# почему:string method
```

26) Что тут за проблемы?

```ruby
my name = "Mr. Stupid"
# ответ:undefined method 'my'
# почему:my_name = "Mr. Stupid"
```

27) Исправьте код чтобы он выполнился:

```ruby
band = "City" + 312
# ответ:band = "City" + " 312"
# почему:
```

28) my_array - массив из значений "ruby" and "rails". Создайте свой массив с такими же значениями.

```ruby
band = "City" + 312
# ответ:a = %w[ruby rails]
# почему:array method
```

29) Создайте пустой массив:

```ruby
# ответ:a = [] or Array.new
# почему:array method
```

30) Создайте массив содержащий значения "алекс мерфи", 33, и 12.5.
```ruby
# ответ: a = ["алекс мерфи", 33, 12.5]
# почему:array method
```

31) Опишите компоненты этого выражения:

```ruby
users = ["john", "alex", "vlad"]
# ответ:Array = [String, String, String]
# почему:
```

32) Что будет результатом выражения:

```ruby
["john", "alex", "vlad"].length()
# ответ:3
# почему:length() - измеряет кол-во элементов
```

33) Массив корректный?

```ruby
name = "sir"
age = 32
[name, age]
# ответ:да
# почему:элементы массива могут быть различных типов
```

34) Получите первый элемент массива.

```ruby
["john", "alex", "vlad"].at(0)
# ответ:["john", "alex", "vlad"].at(0)
# почему:нулевой элемент
```

35) Получите последний элемент массива.

```ruby
["john", "alex", "vlad"]
# ответ:["john", "alex", "vlad"].at(-1)
# почему:последний элемент
```

36) Добавьте строку "я молодец" в конец массива:

```ruby
["кто", "молодец", "?"]
# ответ:["кто", "молодец", "?"] << "я молодец"
# почему:Items can be added to the end of an array by using either push or <<
```

37) Что будет результом выполнения?

```ruby
5 == 5
# ответ:true
# почему:obj == other    #-> true или false
```

38) Что будет результом выполнения?

```ruby
true = 6
# ответ:SyntaxError
# почему:Can't assign to true
```

39) Продемонстрируйте что "руби" полностью эквивалентен "руби".

```ruby
# ответ:"руби" == "руби"
 => true 
# почему:
```

40) Что будет результом выполнения?

```ruby
4 != 4
# ответ:false
# почему:! - not
```

41) Что будет результом выполнения?

```ruby
5 > 4
# ответ:true
# почему:obj > other    #-> true или false
```

42) Что будет напечатано?

```ruby
if 5 > 4
  puts "5 больше 4"
end
# ответ:"5 больше 4"
# почему: if - вернет "5 больше 4" если 5>4
```

43) Что будет напечатано?

```ruby
if 5 < 4
  puts "5 меньше 4"
else
  puts "5 не меньше 4"
end

# ответ:"5 не меньше 4"
# почему:if вернет "5 не меньше 4" если 5не<4
```

44) Что будет напечатано?

```ruby
if "алекс" == "шмалекс"
  puts "алекс это шмалекс"
elsif "череп" == "череп"
  puts "череп и есть череп"
else
  puts "что-то пошло не так"
end
# ответ:"череп и есть череп"
# почему: выполнилось одно из условий
```

45) Что будет напечатано?

```ruby
if 5
  puts "Ну привет"
end
# ответ:Ну привет
# почему:нет оператора сравнения
```

46) Что будет напечатано?

```ruby
if "ruby"
  puts "ruby это круто"
else
  puts "ruby все равно круто"
end
# ответ:ruby это круто
# почему:строка в условии
```

47) Что будет напечатано?

```ruby
if nil
  puts "и правда nil"
else
  puts "насильно nil не будешь"
end
# ответ:насильно nil не будешь
# почему:boolean
```

48) Что будет напечатано?

```ruby
puts "Так тоже можно печатать логические проверки" if true
# ответ:Так тоже можно печатать логические проверки
# почему:
```

49) Что будет напечатано?

```ruby
puts "Так тоже можно проверять если коротко" if false
# ответ: => nil 
# почему: false потому условие не соблюдается
```

50) Какое будет значение X?

```ruby
x = 5
x = x + 1
# ответ:6
# почему:переприсвоение
```

51) Какое будет значение helloer?

```ruby
helloer = "chill"
helloer += "hippie"
# ответ:"chillhippie"
# почему:переприсвоение
```

52) Что будет напечатано?

```ruby
counter = 0
while counter < 3
  puts "Поехали кружиться"
  counter = counter + 1
end
# ответ:Поехали кружиться
Поехали кружиться
Поехали кружиться
# почему:замыкание
```

53) Что будет напечатано?

```ruby
while 3 > 15
  puts "Ну все приплыли"
end
# ответ:=> nil 
# почему:infinite loop
```

54) Что будет напечатано?

```ruby
while true
  puts "Бесконечность тоже конечна, только мы об этом не всегда можем знать."
end
# ответ:"Бесконечность тоже конечна, только мы об этом не всегда можем знать." х много раз
# почему:infinite loop
```
