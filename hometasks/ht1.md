## 1 часть. Овтетьте на вопросы и прокомментируйте почему так происходит.

> Для выполнения задания копируете это себе через fork. В поле "ответ" указываете ответ, в поле "почему" пишете ваше понимание почему это так работает с точки зрения языка.

1) Присвойте переменной x значение 5.

```ruby
# ответ (пример):
x = 5

# почему: все так потому что присвоение в языке идет через одиночный знак равенства, порядок присвоения идет справа налево.
```

2) Какой тип данных имеет это выражение?

```ruby
"Bob"

# ответ: строковый литерал (строка)
# почему: строки заключаются в одинарные или двойные кавычки
```

3) x это строка?

```ruby
x = 5
# ответ: нет, это переменная
# почему: х является переменной, так как после него идет знак присвоения
```

4) Что будет результатом выполнения?

```ruby
"HEL" + "LO"
# ответ: HELLO
# почему: руби сложит две строки
```

5) Что будет результатом выполнения?

```ruby
"Hel".+("lo")
# ответ: Hello
# почему: скобки на являются разделителем, а показывают приоритетность. Точка является разделителем между двумя операторами.
Произойдет сложение 
```

6) Что будет результатом выполнения?

```ruby
"Hel" + 42
# ответ: ошибка
# почему: так как строка и число не могут сложиться
```

7) Что будет результатом выполнения?

```ruby
a = "OH"
b = "mygod"
a + b
# ответ:OHmygod
# почему:сложение двух строк
```

8) Что выведет на экран?

```ruby
my_role = "Teacher"
my_role = "Student"

p my_role
# ответ: Student
# почему: эта переменная находится ниже в коде, а чтение кода идет сверху вниз, поэтому он выведет последнюю
```

9) Получите первую букву из строки:

```ruby
"GeekBrains"
# ответ: "GeekBrains"[0] => G
# почему: доступ к строке или букве по аналогии с массивом
```

10) Получите с первой по треьюю букву из строки:
```ruby
"GeekBrains"
# ответ: "GeekBrains"[0..2]
# почему: получение последовательности, по аналогии с массивом. Использую две точки, так как включительно с третьей буквой строки
```

11) Получите последнюю букву из строки.

```ruby
"GeekBrains"
# ответ: "GeekBrains"[9]
# почему: обращение к строке, как к массиву и запрос последней буквы по индексу.
```

12) Замените букву b на d в строке:

```ruby
"Geekbrains"
# ответ: "Geekbrains".gsub(/b/,'d')
# почему: использовал gsub, а не sub, т.к. в задании не указано, что нужно заменить только "первую встречную" b
```

13) Присвойте значение "хеллоу" переменной howru?

```ruby
# ответ:howru = 'hello'
# почему: = - знак присваивания
```

14) Что будет результатом выполнения?

```ruby
"Пьяный мастер" = never_sleeps
# ответ: ошибка
# почему: присваивание происходит слева направо
```

15) Что будет выведено на экран?

```ruby
chtoto = "budet"
sosvem = netak
puts sosvem
# ответ: ошибка
# почему: переменная не определена
```

16) Какой будет результат?

```ruby
3 + 4
# ответ: 7
# почему: сложение двух чисел
```

17) Какой будет результат?

```ruby
4 * 7
# ответ: 28
# почему: умножение двух чисел
```

18) Какой будет результат?

```ruby
2 ** 3
# ответ: 8
# почему: возведение в степень (двойку возводят в 3 степень)
```

19) Какой будет результат?

```ruby
8 / 2
# ответ: 4
# почему: деление двух чисел
```

20) Какой будет результат?

```ruby
3 / 2
# ответ: 1
# почему: потому что производилось деление без остатка (произошло округление)
```

21) Какой будет результат?

```ruby
3.0 / 2.0
# ответ: 1.5
# почему: деление с остатком, точка показывает остаток
```

22) Какой будет результат?

```ruby
"i am not afraid".upcase()
# ответ: I AM NOT AFRAID
# почему: метод upcase() делает все буквы прописными
```

23) Переведите в нижний регистр.

```ruby
what = "What ArE yOuAR?"
# ответ: what = "What ArE yOuAR?".downcase()
# почему: метод downcase() возвращает значение, написанное строчными буквами
```

24) Конкатенируйте строки:

```ruby
what = "Прекраная "
isthis = "годнота то"
# ответ: word = what + isthis
# почему:  сложение двух переменных
```
25) Переведите число 5 в строку.

```ruby
# ответ: 5.to_s
# почему: to_s - метод переводящий любую переменную в строку
```

26) Что тут за проблемы?

```ruby
my name = "Mr. Stupid"
# ответ: пробел между словами my and name
# почему: в написании переменных, классов, методов пробелы недопустимы
```

27) Исправьте код чтобы он выполнился:

```ruby
band = "City" + 312
# ответ: band = "City" + 312.to_s
# почему: число не может сложиться со строкой.
```

28) my_array - массив из значений "ruby" and "rails". Создайте свой массив с такими же значениями.

```ruby

# ответ: her_array = ['ruby', 'rails']
# почему: массив создается с помощью квадратных скобочек
```

29) Создайте пустой массив:

```ruby
# ответ: my_array = []
# почему:  создан пустой массив
```

30) Создайте массив содержащий значения "алекс мерфи", 33, и 12.5.
```ruby
# ответ: my_array = ['Alex Merfi', 33, 12.5]
# почему: мы можем создать массив с разными типами переменных
```

31) Опишите компоненты этого выражения:

```ruby
users = ["john", "alex", "vlad"]
# ответ: переменная, знак присваивания, массив, содержащий значения массива. Значения являются строками
# почему:
```

32) Что будет результатом выражения:

```ruby
["john", "alex", "vlad"].length()
# ответ: 3

# почему: length() посчитал количество значений внутри массива. Их там три
```

33) Массив корректный?

```ruby
name = "sir"
age = 32
[name, age]
# ответ: думаю, что да
# почему: при вызове в irb [name, age] возвращают указанные величины
```

34) Получите первый элемент массива.

```ruby
["john", "alex", "vlad"]
# ответ: ["john", "alex", "vlad"].first
# почему: метод #first - возвращает значение первого элемента массива
```

35) Получите последний элемент массива.

```ruby
["john", "alex", "vlad"]
# ответ: ["john", "alex", "vlad"].last
# почему: по аналогии возвращает последний элемент массива
```

36) Добавьте строку "я молодец" в конец массива:

```ruby
["кто", "молодец", "?"]
# ответ:  ["кто", "молодец", "?"].push("я молодец")
# почему: метод push прибавляет к массиву новое значение
```

37) Что будет результом выполнения?

```ruby
5 == 5
# ответ: true
# почему: == - знак строгого сравнения. В нашем случае сравнивается два одинаковых числа
```

38) Что будет результом выполнения?

```ruby
true = 6
# ответ: ошибка
# почему: слово true - зарезервировано. Оно не может использоваться в качестве названия переменной
```

39) Продемонстрируйте что "руби" полностью эквивалентен "руби".

```ruby
# ответ: "руби" == "руби"
# почему: строгое сравнение одинаковых строк
```

40) Что будет результом выполнения?

```ruby
4 != 4
# ответ:
# почему: ! - чаще всего используется для изменения функции. 4 = 4 => true, 4 != 4 => falseб так как 4 все-таки равно 4
```

41) Что будет результом выполнения?

```ruby
5 > 4
# ответ: true
# почему: т.к. тождество исполняется верно
```

42) Что будет напечатано?

```ruby
if 5 > 4
  puts "5 больше 4"
end
# ответ: 5 больше 4
# почему: т.к. тождество верное (хотя irb сначала возвращает 5 больше 4 => nil)
```

43) Что будет напечатано?

```ruby
if 5 < 4
  puts "5 меньше 4"
else
  puts "5 не меньше 4"
end

# ответ: 5 не меньше 4
# почему: потому что if не может исполниться по логике вещей - 5 не может быть меньше 4. Поэтому исполняется конструкция else
```

44) Что будет напечатано?

```ruby
if "алекс" == "шмалекс"
  puts "алекс это шмалекс"
elsif "череп" == "череп"
  puts "череп и есть череп"
else
  puts "что-то пошло не так"
end
# ответ: "череп и есть черпе"
# почему: в конструкции elsif "череп" == "череп" идет строгое сравнение, которое верно, так как это одинаковые строки. Поэтому программа заканчивается на elsif 
```

45) Что будет напечатано?

```ruby
if 5
  puts "Ну привет"
end
# ответ: Ну привет
# почему: соблюдаются условия
```

46) Что будет напечатано?

```ruby
if "ruby"
  puts "ruby это круто"
else
  puts "ruby все равно круто"
end
# ответ: выведется puts "ruby это круто"
# почему: исполняются все условия
```

47) Что будет напечатано?

```ruby
if nil
  puts "и правда nil"
else
  puts "насильно nil не будешь"
end
# ответ:  puts "насильно nil не будешь"
# почему: выражение не является nil?
```

48) Что будет напечатано?

```ruby
puts "Так тоже можно печатать логические проверки" if true
# ответ: puts "Так тоже можно печатать логические проверки"
# почему: в конечном итоге это правда
```

49) Что будет напечатано?

```ruby
puts "Так тоже можно проверять если коротко" if false
# ответ: nil
# почему: ничего не вернет, так можно, но не для лжи
```

50) Какое будет значение X?

```ruby
x = 5
x = x + 1
# ответ: 6
# почему: мы задаем переменную х, а затем перезадаем ее, используя предыдущий аргумент х
```

51) Какое будет значение helloer?

```ruby
helloer = "chill"
helloer += "hippie"
# ответ: chillhippie
# почему: к уже заданной переменное прибавляем дополнительный аргумент
```

52) Что будет напечатано?

```ruby
counter = 0
while counter < 3
  puts "Поехали кружиться"
  counter = counter + 1
end
# ответ: "Поехали кружиться" "Поехали кружиться" "Поехали кружиться"
# почему: счетчик начинается с нуля и будет исполняться до тех пор, пока будет меньше 3, соответственно 3 раза
```

53) Что будет напечатано?

```ruby
while 3 > 15
  puts "Ну все приплыли"
end
# ответ: false or nil
# почему: Эта программа логически не работает. 3 никогда не будет больше 15
```

54) Что будет напечатано?

```ruby
while true
  puts "Бесконечность тоже конечна, только мы об этом не всегда можем знать."
end
# ответ: Сначала был ответ true, но в irb получился бесконечный цикл
# почему: Так как это правда, то этому не будет конца и нет ограничивающих программ
```
